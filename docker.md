# Erstmaliger Start mit Build
docker compose up --build

# Normaler Start
docker compose up

# Start im Hintergrund
docker compose up -d

# Stoppen
docker compose down

# Logs anzeigen
docker compose logs -f

#  Läuft der Container? 
docker compose ps
